# Python
__pycache__/
*.py[cod]
*.pyo
*.pyd
.Python
*.egg-info/
dist/
build/
*.egg
.eggs/

# Node / card build (card/ is a standalone project; its built bundle is copied
# to custom_components/choreflow/www/ and committed — dist/ itself is ignored)
node_modules/
*.tsbuildinfo

# Virtual environments
venv/
.venv/
env/
.env

# Secrets — NEVER commit these
credentials.json
*.json.secret
secrets.yaml
*.token
*token-cache*
.storage/
token*.pickle

# HA runtime state
home-assistant_v2.db
home-assistant.log*
.HA_VERSION

# ChoreFlow runtime artifacts
choreflow.db
choreflow_exports/

# Test artifacts
.pytest_cache/
.coverage
htmlcov/
coverage.xml
*.coveragerc

# Linting/type checking
.mypy_cache/
.ruff_cache/

# detect-secrets — .secrets.baseline IS committed (pre-commit needs it)

# IDE / tooling
.vscode/
.idea/
.claude/
*.swp
*.swo

# OS
.DS_Store
Thumbs.db
